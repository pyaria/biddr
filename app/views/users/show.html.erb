<h1>Welcome <%= @user.full_name %>!</h1>
<hr>
<h3>Here are all your bids so far</h3>
<% @bids.each do |bid| %>
  <p><%= bid.amount %> on <%= link_to "#{find_auction(bid.auction_id)}", auction_path(bid.auction_id) %> (<%= bid.created_at %>)</p>
<% end %>
<h3>Go win those bidding wars!</h3>
